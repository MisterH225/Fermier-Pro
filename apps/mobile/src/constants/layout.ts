/** Hauteur visuelle de la barre de navigation flottante (pill + icônes). */
export const NAV_BAR_HEIGHT = 80;

/** Marge supplémentaire sous le contenu scrollable. */
export const BOTTOM_INSET_BASE = 16;

/** Hauteur estimée de la barre de saisie chat (hors inset navbar). */
export const CHAT_INPUT_BAR_HEIGHT = 56;
