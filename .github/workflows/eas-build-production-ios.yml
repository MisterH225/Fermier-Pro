name: EAS Build Production iOS

on:
  push:
    branches: [main]
    paths:
      - ".github/eas-build-production-ios.env"
      - ".github/workflows/eas-build-production-ios.yml"
  workflow_dispatch:
    inputs:
      message:
        description: Message du build EAS (optionnel)
        required: false
        default: ""

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build & submit iOS production (TestFlight)
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        working-directory: apps/mobile
        run: |
          if [[ -z "${EXPO_TOKEN:-}" ]]; then
            echo "::error::EXPO_TOKEN non configuré — build EAS impossible."
            exit 1
          fi
          MSG="${{ github.event.inputs.message }}"
          if [[ -z "$MSG" ]]; then
            MSG="iOS production build (${GITHUB_SHA:0:7}) — $(date -u +%Y-%m-%dT%H:%MZ)"
          fi
          npx eas build \
            --platform ios \
            --profile production \
            --non-interactive \
            --auto-submit-with-profile=production \
            --message "$MSG"
