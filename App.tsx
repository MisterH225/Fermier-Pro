/**
 * Point d’entrée attendu par `node_modules/expo/AppEntry.js` (`import App from '../../App'`)
 * lorsque `expo` est hoisté à la racine du monorepo. L’app réelle vit dans `apps/mobile/`.
 */
import App from "./apps/mobile/App";

export default App;
